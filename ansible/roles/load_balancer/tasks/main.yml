- name: Install nginx
  apt:
    name: nginx
    state: present
    update_cache: yes

- name: Create nginx config
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/sites-enabled/default

- name: Restart nginx
  shell: |
    nginx -s stop || true
    nginx
