# Application version to deploy
APP_VERSION=v1.0.0

# Port configuration for Blue/Green environments
# Blue runs on 5000, Green runs on 5001
APP_PORT=5000

# Ansible variable example (used via CLI)
# ansible-playbook deploy.yml -e "app_version=v1.0.0"
